{
  "name": "frontbook-react-example",
  "version": "1.0.0",
  "description": "Frontbook react example (empty project)",
  "browser": "true",
  "scripts": {
    "start": "frontbook-react dev",
    "dev": "npx prestart && frontbook-react dev",
    "export": "npx prestart && frontbook-react export",
    "watch": "npx prestart && frontbook-react watch",
    "prestart": "npx prestart"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "frontbook-react": "^1.1.32"
  }
}
